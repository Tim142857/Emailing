<div id="welcome">
  <div class="container">
    <h1>Welcome!</h1>
    <hr/>
    <p>This is a page that only logged-in people can visit. Don't you feel special? Try clicking on a button below to do some things you can't do when you're logged out.</p>
    <div class="buttons">
      <a class="btn btn-info" href="/account/profile">Update my email</a>
      <button class="btn btn-outline-info ml-2" @click="clickOpenDashboardModalButton('dashboard')">Open a modal</button>
    </div>
  </div>

      <!-- <modal class="example-modal" v-if="dashboardModalVisible" @close="closeDashboardModal('dashboard')" v-cloak>
      <div class="modal-header">
      <h2 class="modal-title">Import de fichiers</h2>
      <hr/>
      <button type="button" class="modal-close-button" data-dismiss="modal" aria-label="Close"></button>
    </div>
    <div class="modal-footer flex-row-reverse justify-content-between">
    <button type="button" data-dismiss="modal" class="btn btn-info">Pas Maintenant !</button>
    <button type="button" data-dismiss="modal" class="btn btn-outline-info" @click="clickOpenDashboardModalButton('upload')">Importer des fichiers</button>
  </div>
</modal> -->

<!-- <modal class="example-modal" v-if="uploadModalVisible" @close="clickOpenDashboardModalButton('upload')" v-cloak>
<div class="modal-header">
<h2 class="modal-title">Import de fichiers</h2>
<hr/>
<button type="button" class="modal-close-button" data-dismiss="modal" aria-label="Close"></button>
</div>
<div class="modal-body flex-row-reverse justify-content-between">
<form method="POST" action="/upload" id="uploadForm" enctype="multipart/form-data">
<div class="form-group">
<input type="file" name="uploadedFile" class="form-control-file" id="uploadedFile" aria-describedby="fileHelp">
<small id="fileHelp" class="form-text text-muted">Choisir un fichier exporté au format csv à partir d'Excel</small>
</div>
<div class="form-group">
<button type="submit" id="upload-btn" class="btn btn-primary">Envoyer</button>
</div>
</form>
</div>
</modal> -->
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
